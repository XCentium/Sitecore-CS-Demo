@using Sitecore.Links;
@using Sitecore.Mvc;
@using Sitecore.Resources.Media;
@inherits Glass.Mapper.Sc.Web.Mvc.GlassView<IEnumerable<CSDemo.Models.Marquee.CarouselItem>>
<!-- ==========================
JUMBOTRON - START
=========================== -->
@if (Model == null)
{
    return;
}
<section class="content jumbotron">
    @if (IsInEditingMode)
    {
        <div id="div-carousel" class="nav-inside">
            @foreach (var slide in Model.Where(t => t != null))
            {
                if (slide.Image == null) { continue; }
                    using (BeginEditFrame(slide, "Edit Hero Slide", x => x.Content, x => x.Image))
                    {
                    <div class="item slide-1" style="background-image: url(@slide.Image.Src)">
                        <div class="slide-mask"></div>
                        <div class="slide-body">
                            <div class="container">
                                @Editable(slide, x => x.Content)
                            </div>
                        </div>
                    </div>
                }
            }
        </div>
    }
    else
    {
        <div id="homepage-1-carousel" class="nav-inside owl-animation">
            @foreach (var slide in Model.Where(t => t != null))
            {
                <div class="item slide-1" style="background-image: url(@slide.Image.Src)">
                    <div class="slide-mask"></div>
                    <div class="slide-body">
                        <div class="container">
                            @Html.Raw(@slide.Content)
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</section>
<!-- ==========================
JUMBOTRON - END
=========================== -->
